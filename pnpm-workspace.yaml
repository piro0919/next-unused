# pnpm 11 ignores pnpm.onlyBuiltDependencies in package.json and fails the
# install until every package with a build script is listed here. Both of these
# need to run: esbuild fetches its binary, lefthook installs the git hooks.
allowBuilds:
  esbuild: true
  lefthook: true
